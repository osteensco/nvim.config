local arrow = require("arrow")

arrow.setup({
    show_icons = true,
    leader_key = ";",
    buffer_leader_key = "m",
})

-- require("arrow.git").refresh_git_branch()
-- require("arrow.persist").load_cache_file()
